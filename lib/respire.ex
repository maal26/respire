defmodule Respire do
  @moduledoc """
  A Redis-compatible server built on plain OTP.

  Speaks RESP2 over TCP and implements a subset of the Redis command set,
  using only the standard library.
  """
end
